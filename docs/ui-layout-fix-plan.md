# UI Layout Fix План — Категории на мобильных

**Дата:** 2025-12-25
**Проблема:** Текст и кнопки перекрываются в списке категорий на узких экранах
**Статус:** Планирование

---

## 📍 Проблема

На странице `/categories` в секции "Текущие категории" содержимое элементов списка **перекрывается**:
- Названия категорий наезжают на теги "DEFAULT"
- Текст баланса перекрывает кнопки "Редактировать" и "В архив"
- На мобильных устройствах (iPhone SE) компоновка становится нечитаемой

**Причина:** Flex-контейнер без поддержки переноса на узких экранах + недостаточный padding.

---

## 🎯 Решение

### Цель
Переделать макет категорий так, чтобы на мобильных (< 768px) кнопки и текст располагались **в колонке** (flex-col), а на десктопе (≥ 768px) — **в строке** (flex-row с justify-between).

### Стратегия
1. Изменить flex-контейнер на `flex flex-col md:flex-row`
2. Добавить `md:items-start` для выравнивания на десктопе
3. Увеличить padding с `py-2` на `py-3` для комфортного расстояния между строками
4. Убедиться, что `gap-3` достаточно для разделения левой и правой части

---

## 🔧 Конкретные изменения

### Файл: `pages/categories.vue`

#### Изменение 1: Расходы (Expense Categories)
**Строка 229**

**Было:**
```html
<li
  v-for="category in expenseCategories"
  :key="category.id"
  class="flex items-start justify-between gap-3 py-2"
>
```

**Станет:**
```html
<li
  v-for="category in expenseCategories"
  :key="category.id"
  class="flex flex-col md:flex-row md:items-start md:justify-between gap-3 py-3"
>
```

#### Изменение 2: Доходы (Income Categories)
**Строка 275**

**Было:**
```html
<li
  v-for="category in incomeCategories"
  :key="category.id"
  class="flex items-start justify-between gap-3 py-2"
>
```

**Станет:**
```html
<li
  v-for="category in incomeCategories"
  :key="category.id"
  class="flex flex-col md:flex-row md:items-start md:justify-between gap-3 py-3"
>
```

---

## 📊 Результаты

### На мобильных (< 768px)
```
┌─────────────────────┐
│ Кафе         DEFAULT│
│ Баланс: -500.00 ₽   │
│ ┌─────────────────┐ │
│ │ Редактировать   │ │
│ └─────────────────┘ │
│ ┌─────────────────┐ │
│ │ В архив         │ │
│ └─────────────────┘ │
└─────────────────────┘
```

### На десктопе (≥ 768px)
```
┌───────────────────────────────────────────┐
│ Кафе          Баланс: -500.00 ₽           │
│ DEFAULT       Редактировать  В архив       │
└───────────────────────────────────────────┘
```

---

## ✅ Чек-лист реализации

- [ ] Прочитать полный файл `pages/categories.vue`
- [ ] Найти оба элемента `<li>` в строках 226-262 и 272-308
- [ ] Заменить `class="flex items-start justify-between gap-3 py-2"` на `class="flex flex-col md:flex-row md:items-start md:justify-between gap-3 py-3"` в обоих элементах
- [ ] Запустить `npm run dev` и проверить на мобильном разрешении
- [ ] Проверить на десктопе (≥ 768px)
- [ ] Запустить тесты: `npm run test`
- [ ] Обновить `docs/status.md` с отметкой о завершении

---

## 📝 Примечания

- **Breakpoint `md`** в Tailwind = 768px (iPad и выше)
- **`flex-col`** = flex-direction: column (вертикальное расположение)
- **`md:flex-row`** = на экранах ≥ 768px вернуться в горизонтальное расположение
- **`py-3`** = padding-top и padding-bottom по 0.75rem (хватит для переноса текста)
- **Сбор Tailwind:** классы автоматически соберутся при сборке проекта

---

## 🚀 Команды для тестирования

```bash
# Запустить dev-сервер
npm run dev

# Открыть DevTools мобильного вида (F12 → toggle device toolbar)
# Выбрать iPhone SE 2022 (375px ширина)

# После изменений запустить тесты
npm run test

# Собрать проект
npm run build
```

---

**Автор плана:** Claude Code
**Приоритет:** Средний (UI bug, влияет на юзабилити)
**Сложность:** Простая (2 строчки изменений)
